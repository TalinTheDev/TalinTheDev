# Use a Node.js base image (version 22.14)
FROM node:22.14.0-bookworm 

# Install pnpm
RUN npm install -g pnpm

# Set the working directory
WORKDIR /workspace

# Expose any required ports (e.g., frontend)
EXPOSE 80